# H2 Optimize Backend Environment Configuration

# Server Configuration
NODE_ENV=development
PORT=5000

# Database Configuration
MONGODB_URI=mongodb+srv://yugbuha001:admin@mycluster1.cehsoj3.mongodb.net/?retryWrites=true&w=majority&appName=MyCluster1

# CORS Configuration
CLIENT_URL=http://localhost:3000

# Optional: MongoDB Atlas connection (for production)
# MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/h2-optimize?retryWrites=true&w=majority

# Optional: JWT Secret (if authentication is added later)
# JWT_SECRET=your-super-secret-jwt-key-here

# Optional: External API keys (for real renewable energy data)
# WEATHER_API_KEY=your-weather-api-key
# SOLAR_API_KEY=your-solar-data-api-key
